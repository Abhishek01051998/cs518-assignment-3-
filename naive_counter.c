#include <stdio.h>
#include <stdlib.h>
#include <pthread.h>
#include <sys/time.h>

/*
1. Generate n threads using pthread library
2. For every thread call a function(every x times) that increments counter by 1
*/

#define COUNTER_VALUE (1UL << 24)

int main(int argc, char** argv) {

	return 0;
}
